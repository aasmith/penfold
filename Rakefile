# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'penfold' do |p|
  p.developer('Andrew A. Smith', 'andy@tinnedfruit.org')
  p.readme_file = "README.rdoc"
  p.extra_deps.push *[%w(nokogiri >=0), %w(penfold >=0)]
end

Hoe.add_include_dirs '.'

# vim: syntax=Ruby
